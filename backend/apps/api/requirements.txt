fastapi==0.120.4
uvicorn[standard]==0.38.0
pydantic==2.12.4
pydantic-settings==2.13.0
psycopg[binary]==3.2.12
psycopg-pool==3.2.6
python-multipart==0.0.20

# PDF text extraction (PRD Section 6). Only imported by app/services/parsing.py.
pymupdf==1.28.2

# Local embeddings via ONNX — no API key, no network at inference time.
# Only imported by app/services/embedding.py. Downloads the model (~120MB) on
# first use and caches it. Model choice is documented in PROGRESS.md.
fastembed==0.8.0
# Direct dependency for the local neural relevance training pipeline. Kept
# explicit rather than relying on fastembed to pull it transitively.
numpy==2.5.2

# LLM access. OpenRouter is OpenAI-compatible, so one HTTP client is enough --
# no provider SDK. Only imported by app/services/llm.py.
httpx==0.28.1

# Vector store. `chromadb-client` is the thin HTTP-only client, not the full
# `chromadb` server package: the server runs as a container (docker-compose),
# so pulling in its embedded engine would add kubernetes, pypika and a second
# uvicorn for nothing. Version tracks the server image in docker-compose.yml --
# bump both together. Only imported by app/services/vector_store.py, and only
# lazily, so the API still starts if it is absent.
chromadb-client==1.5.9

# Tests
pytest==8.4.2

# Clerk session verification. Tokens are RS256, checked locally against the
# published JWKS, so no Clerk SDK and no per-request call to Clerk. The
# [crypto] extra pulls `cryptography`, which is what actually verifies RS256 —
# without it PyJWT accepts the token shape and rejects every signature.
# Only imported by app/core/auth.py.
pyjwt[crypto]==2.10.1
